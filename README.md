# Bricio Blancas Salgado Mission Control Portfolio


## Run Locally

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

## Content To Update

- Replace placeholder contact URLs in `src/data.js`.
- Add the real resume as `public/resume.pdf` or update the resume link in `src/App.jsx`.
- Replace generic GitHub and demo URLs in the mission cards when project links are available.

## Generated Asset

The hero background was generated for this project and saved at `src/assets/mission-control-hero.png`.
